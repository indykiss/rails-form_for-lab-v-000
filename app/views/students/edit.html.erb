<%= @students_each do |student|%>

  <%= student.label :first_name %>
    <%= student.text_field :first_name%>
  <%= student.label :last_name %>
    <%= student.text_field :room_number %>

<%= student.submit "Update Student" %>

<% end %>
